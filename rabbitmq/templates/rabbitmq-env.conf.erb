
# defaults to empty string, change only to bind to one interface
NODE_IP_ADDRESS=<%= bind %>

# defaults to 5672
NODE_PORT=<%= port %>

# important for clustering defaults to rabbit@$hostname
NODENAME=<%= nodename %>
